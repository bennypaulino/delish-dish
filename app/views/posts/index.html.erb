<h1>Home page for DELISH-dish</h1>
<% @posts.each do |post| %>
  <div>
    <%= link_to (image_tag post.image.url(:medium), class:'img-responsive'), post_path(post) %>
    <%= post.caption %>
  </div>
<% end %>

<%= link_to 'New Post', new_post_path %>